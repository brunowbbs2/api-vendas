apiVersion: apps/v1
kind: Deployment
metadata:
  name: produtos-service
  namespace: ecommerce
spec:
  replicas: 1
  selector:
    matchLabels:
      app: produtos-service
  template:
    metadata:
      labels:
        app: produtos-service
    spec:
      initContainers:          # rodam antes do container principal, um de cada vez
        - name: espera-eureka
          image: busybox:1.36
          command: ['sh', '-c', 'until nc -z eureka-server 8761; do sleep 2; done']
        - name: espera-config-server
          image: busybox:1.36
          command: ['sh', '-c', 'until nc -z config-server 8888; do sleep 2; done']
      containers:
        - name: produtos-service
          image: produtos-service:1.0
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8081
          env:
            - name: SPRING_CONFIG_IMPORT
              value: "optional:configserver:http://config-server:8888"
            - name: SPRING_PROFILES_ACTIVE
              value: "docker"
            - name: EUREKA_CLIENT_SERVICEURL_DEFAULTZONE
              value: "http://eureka-server:8761/eureka/"
          readinessProbe:
            tcpSocket: { port: 8081 }
            initialDelaySeconds: 20
            periodSeconds: 10
---
apiVersion: v1
kind: Service
metadata:
  name: produtos-service
  namespace: ecommerce
spec:
  selector:
    app: produtos-service
  ports:
    - port: 8081
      targetPort: 8081